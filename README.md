# 🍪 Cookie Editor 

**Інструмент для QA-інженерів та розробників для налагодження, швидкого заповнення даних та тестування логіки скорингу на проектах HAF/Embody.**

Цей Userscript додає плаваючу панель на сторінку, яка дозволяє в реальному часі редагувати JSON-дані користувача (Cookies/LocalStorage), автоматично розраховувати складні метрики (BMI, Metabolism, Somatotype) та керувати станом додатку.

---

## ✨ Основні можливості

* **📱 Mobile-First Design:** Повністю адаптивний інтерфейс. Панель займає 90% ширини на мобільних пристроях, має зручні кнопки та автоматично позиціонується, щоб не виходити за межі екрану.
* **⚡ Quick Fill (Швидке заповнення):** Миттєве заповнення даних користувача під конкретні фанели (`f1f123r`, `c1f23r`, `w2f123r` тощо).
* **📊 Wellness Profile:** Зручний UI для редагування зросту, ваги, віку та цільової ваги з автоматичним перерахунком BMI та повідомлень про цілі.
* **🧠 Розумні розрахунки (Logic Engine):** Автоматичне визначення **Metabolism**, **Somatotype**, **Lifestyle** та **Fitness Level** на основі введених даних.
* **🍪 Cookie & Storage Management:** Керування куками `sandbox_test`, `screens` та таймерами знижок (Discount).
* **🔍 JSON Editor:** Повноцінний редактор JSON з підсвічуванням синтаксису та пошуком по тексту.

---

## 🔗 Installation

1. Install [Tampermonkey extension](https://www.tampermonkey.net/).
2. Enable [Developer mode](https://www.tampermonkey.net/faq.php?locale=en#Q209) to use userscripts.
3. [Install](https://raw.githubusercontent.com/qa-helloembody/Cookie-Editor/main/cookie-editor.user.js) userscript

---

## 🛠 Функціонал та Логіка

### 1. Wellness Profile (Stats/Meta)
Цей блок дозволяє швидко змінювати фізичні параметри користувача.

* **Height / Cur.W / Tar.W / Age:** Поля для введення зросту (см), поточної ваги (кг), цільової ваги (кг) та віку.
    * *Примітка:* При зміні цих полів автоматично оновлюється JSON та перераховується BMI.
    * Значення BMI зберігається в `sessionStorage` під ключем `bmi`.
* **Goal Msg (Повідомлення про ціль):**
    Логіка формує повідомлення на основі різниці ваги у відсотках (`diff`).
    * `Uh-oh! Low weight alert!` — якщо вага нижче норми (BMI < 18.5).
    * `CHALLENGING GOAL: lose X%` — втрата ваги >= 25%.
    * `HEALTH BENEFITS: lose X%` — втрата ваги 10-24%.
    * `EASY WIN: lose X%` — втрата ваги 1-9%.
    * `EASY WIN: gain X%` — набір ваги 0-9%.
    * `CHALLENGING GOAL: gain X%` — набір ваги 10-24%.
    * `AUDACIOUS GOAL: gain X%` — набір ваги >= 25%.
* **Age Presets:** Швидкі кнопки для встановлення вікових груп (залежать від поточного фанелу).
* **Physical Parameters:** Кнопки для вибору `Weight Change` (тенденція ваги) та `Body Fat` (тип тіла). Ці параметри впливають на розрахунок метаболізму.

### 2. Matrix Calculations (Lifestyle, Metabolism, Somatotype)

Скрипт використовує складну матрицю рішень для визначення параметрів:

#### A. Lifestyle (Спосіб життя)
Розраховується на перетині `Daily Activity` та `Walking Activity`.

| Daily \ Walking | < 1 hour | 1-2 hours | > 2 hours |
| :--- | :--- | :--- | :--- |
| **Sedentary** | Sedentary | Sedentary | Moderately Active |
| **Lightly** | Sedentary | Lightly Active | Moderately Active |
| **Moderately** | Lightly Active | Moderately Active | Active |
| **Very** | Moderately Active | Active | Highly Active |
| **Highly** | Highly Active | Highly Active | Highly Active |

#### B. Metabolism & Somatotype
Розраховуються на основі трьох факторів: **BMI Range**, **Weight Change Pattern**, **Body Fat Level**.

* **BMI Ranges:** Under (<18.5), Healthy (18.5-24.9), Over (25-29.9), Obese (30+).
* **Fat Levels:** Low (Toned/Slim), Mid (Medium), High (Plus/Overweight).
* *Приклад логіки:*
    * Якщо BMI `Healthy` + Pattern `Struggles to gain` + Fat `Low` -> **Meta: Fast, Soma: Ectomorph**.
    * Якщо BMI `Obese` -> **Meta: Slow, Soma: Endomorph**.

### 3. Score Calculations
Відображає внутрішні бали, які використовуються алгоритмом для підбору плану тренувань.

* **Fitness Score:** Розраховується як середньозважене значення відповідей про активність (Exercise, Stairs, Squats).
* **Plan Score:** Формула, що враховує гнучкість, досвід в йозі/пілатесі та рівень активності.
    * Визначає складність плану: *"Gentle"* vs *"Basic"*.
* **Assigned Plan:** Показує, який план буде призначено користувачеві (наприклад, *Gentle Wall Pilates Plan*).

### 4. System Controls
* **Sandbox Cookie:** Перемикач для куки `sandbox_test` (True/Delete).
    * *Особливість:* Автоматично приховується для фанелу `w2f123r`.
* **Discount Controls:** Кнопки для тестування таймерів знижки.
    * `Activate Discount`: Встановлює таймер закінчення знижки на +10 хвилин від поточного часу.
    * `Expire Now`: Робить знижку простроченою миттєво.
* **Clear Site Data:** Очищає всі Cookies, LocalStorage та SessionStorage для поточного домену.

---

## ⌨️ Використання

1.  **Відкриття/Закриття:** Панель можна згорнути, натиснувши кнопку `-` у заголовку. Стан (згорнуто/розгорнуто) та позиція вікна зберігаються після перезавантаження.
2.  **Quick Fill:**
    * Натисніть кнопку `Quick Fill`.
    * Скрипт перевірить `funnelId` в URL.
    * Якщо це `c1f23r`, завантажиться шаблон для Chair Yoga.
    * Якщо це `w1f...` або `w2f...`, завантажиться шаблон для Wall Pilates.
    * В іншому випадку завантажиться дефолтний Fitness шаблон.
3.  **Редагування JSON:** Ви можете вручну змінити будь-яке значення в текстовому полі JSON. Зміни застосовуються автоматично при втраті фокусу або натисканні `Save`.
4.  **Пошук:** Використовуйте поле пошуку над JSON для швидкої навігації по ключах (наприклад, введіть "email").

---
